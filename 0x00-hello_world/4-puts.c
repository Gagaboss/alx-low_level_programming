#include <stdio.h>
/**
 * main - prints "A program that prints a line with puts functions"
 * return: 0 (success)
 */
int (void) main
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
